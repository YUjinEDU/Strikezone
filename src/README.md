# Python Source (v2+)

`src/`는 **향후(딥러닝/데이터 파이프라인/공통 유틸)**을 위한 신규 Python 코드 공간입니다.

## 현재
- `src/core/`: 공통 유틸/타입(추후 확장)
- `src/dataset/`: 데이터 파이프라인(유튜브 매니페스트/프레임 추출 등) (추후 확장)

## 레거시 코드
기존 연구 코드는 `src_v1/`에 보존되어 있습니다.


